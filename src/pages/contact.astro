---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Kontak">
  <section class="mt-12 text-center max-w-xl mx-auto">
    <h1 class="text-3xl font-bold mb-4">Hubungi Saya</h1>
    <p class="text-muted mb-8">
      Ingin berdiskusi, bertanya, atau kerja sama? Kirim pesan melalui form di bawah atau hubungi langsung lewat media sosial.
    </p>

    <form class="space-y-6 text-left">
      <div>
        <label for="name" class="block text-sm mb-1 text-muted">Nama</label>
        <input
          type="text"
          id="name"
          name="name"
          placeholder="Nama lengkap"
          class="w-full px-4 py-2 rounded-lg bg-zinc-900 text-white border border-white/10 focus:outline-none focus:border-cyan-400"
        />
      </div>
      <div>
        <label for="email" class="block text-sm mb-1 text-muted">Email</label>
        <input
          type="email"
          id="email"
          name="email"
          placeholder="nama@email.com"
          class="w-full px-4 py-2 rounded-lg bg-zinc-900 text-white border border-white/10 focus:outline-none focus:border-cyan-400"
        />
      </div>
      <div>
        <label for="message" class="block text-sm mb-1 text-muted">Pesan</label>
        <textarea
          id="message"
          name="message"
          rows="5"
          placeholder="Tulis pesanmu di sini..."
          class="w-full px-4 py-2 rounded-lg bg-zinc-900 text-white border border-white/10 focus:outline-none focus:border-cyan-400"
        ></textarea>
      </div>
      <button
        type="submit"
        class="w-full py-3 rounded-lg bg-white text-black font-semibold hover:bg-gray-200 transition"
      >
        Kirim Pesan
      </button>
    </form>

    <p class="text-muted text-sm mt-8">
      Atau kirim langsung ke email: <a href="mailto:emailkamu@email.com" class="underline">emailkamu@email.com</a>
    </p>
  </section>
</BaseLayout>

<style>
  .text-muted {
    color: var(--color-muted);
  }
</style>
